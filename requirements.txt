# Core scientific computing dependencies
numpy>=1.23.0
numba>=0.58.0
matplotlib>=3.8.0
scipy>=1.11.0  # IMPORTANT: Required by numba for JIT compilation of certain operations
pyyaml>=6.0

# Visualization
cmasher>=1.7.0

# MPI support (CRITICAL for distributed computing)
# Note: mpi4py must be compiled against your system's MPI installation
# On HPC systems like Frontier/Andes, install with:
#   MPICC=cc pip install --no-binary mpi4py mpi4py
mpi4py>=3.1.0

# HDF5 support for data I/O (optional, for HDF5 file support)
h5py>=3.9.0
